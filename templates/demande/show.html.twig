{% extends 'base.html.twig' %}

{% block title %}Demande{% endblock %}

{% block body %}
    <h1>Demande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdDemande</th>
                <td>{{ demande.idDemande }}</td>
            </tr>
            <tr>
                <th>Datepostulation</th>
                <td>{{ demande.datepostulation }}</td>
            </tr>
            <tr>
                <th>Cv</th>
                <td>{{ demande.cv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_demande_index') }}">back to list</a>

    <a href="{{ path('app_demande_edit', {'idDemande': demande.idDemande}) }}">edit</a>

    {{ include('demande/_delete_form.html.twig') }}
{% endblock %}
