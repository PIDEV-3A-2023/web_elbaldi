<!DOCTYPE html>

<title>Commentaire</title>

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>id_commentaire</th>
                <td>{{ commentaire.id_commentaire }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>Date_comm</th>
                <td>{{ commentaire.date_comm ? commentaire.date_comm|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_edit', {'id_commentaire': commentaire.id_commentaire}) }}">edit</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
