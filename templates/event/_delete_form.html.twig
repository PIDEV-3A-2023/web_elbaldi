<form method="post" action="{{ path('app_event_delete', {'idEvent': event.idEvent}) }}" onsubmit="Êtes-vous sûr de vouloir supprimer cet évènement ?;">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.idEvent) }}">
	<button class="btn">Supprimer</button>
</form>
