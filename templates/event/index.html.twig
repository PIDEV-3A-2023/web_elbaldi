{% extends 'baseBack.html.twig' %}

{% block title %}Event index{% endblock %}
{% block body %}

    <h1>Event List</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdEvent</th>
                <th>TitleEvent</th>
                <th>ImageEvent</th>
                <th>DescriptionEvent</th>
                <th>TimeEvent</th>
                <th>Organisation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for event in events %}
            <tr>
                <td style="width:10px;">{{ event.idEvent }}</td>
                <td style="width:10px;">{{ event.titleEvent }}</td>
                <td style="width:10px;"> <div class="crop"><img src="{{ asset('/uploads/'~ event.imageEvent) }}" alt=""/></div></td>
                <td style="width:10x;">{{ event.descriptionEvent }}</td>
                <td style="width:10px;">{{ event.timeEvent ? event.timeEvent|date('Y-m-d H:i:s') : '' }}</td>
                <td style="width:10px;">{{ event.Organisation }}</td>
                <td>
                    {{ include('event/_delete_form.html.twig') }}
                    <a href="{{ path('app_event_edit', {'idEvent': event.idEvent}) }}">Edit</a>
                </td>
            </tr>
       
        {% endfor %}
        </tbody>
    </table>
 <!-- Template Javascript -->
    <script src="{{asset('https://code.jquery.com/jquery-3.4.1.min.js')}}"></script>
     <script src="{{asset('assetsB/lib/waypoints/waypoints.min.js')}}"></script>
      <script src="{{asset('assetsB/lib/tempusdominus/js/moment.min.js')}}"></script>
    <script src="{{asset('assetsB/lib/tempusdominus/js/moment-timezone.min.js')}}"></script>
     <script src="{{asset('https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js')}}"></script>
    <script src="{{asset('assetsB/lib/chart/chart.min.js')}}"></script>
    <script src="{{asset('assetsB/lib/easing/easing.min.js')}}"></script>
   
    <script src="{{asset('assetsB/lib/owlcarousel/owl.carousel.min.js')}}"></script>
   
    <script src="{{asset('assetsB/lib/tempusdominus/js/tempusdominus-bootstrap-4.min.js')}}"></script>
    <script src="{{ asset ('assetsB/js/main.js')}}"></script>
  <!-- JavaScript Libraries -->
    <a href="{{ path('app_event_new1') }}">Create new</a>
{% endblock %}
