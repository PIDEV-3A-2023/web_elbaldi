{% extends 'baseBack.html.twig' %}
{% block title %}
Panier
{% endblock %}


{% block body %}
    <h1>Panier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPanier</th>
                <td>{{ panier.idPanier }}</td>
            </tr>
            <tr>
                <th>NombreArticle</th>
                <td>{{ panier.nombreArticle }}</td>
            </tr>
            <tr>
                <th>TotalPanier</th>
                <td>{{ panier.totalPanier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_panier_index') }}">back to list</a>

    <a href="{{ path('app_panier_edit', {'idPanier': panier.idPanier}) }}">edit</a>

    {{ include('panier/_delete_form.html.twig') }}
{% endblock %}
