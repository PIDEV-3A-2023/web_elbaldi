{{form_start(form,{'attr':{'novalidate':'novalidate'}})}}

<div class="form-group row">
	<label for="{{ form.nom.vars.id }}" class="col-sm-2 col-form-label">{{ form.nom.vars.label }}</label>
	<div class="col-sm-8">
		{{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
		{{ form_errors(form.nom) }}
	</div>
</div>
<div class="form-group row">
	<label for="{{ form.difficulte.vars.id }}" class="col-sm-2 col-form-label">{{ form.difficulte.vars.label }}</label>
	<div class="col-sm-8">
		{{ form_widget(form.difficulte, {'attr': {'class': 'form-control'}}) }}
		{{ form_errors(form.difficulte) }}
	</div>
</div>
<div class="form-group row text-center">
	<div class="col-sm-2"></div>
	<div class="col-sm-10 buttons-group">
		<button class="btn btn-success custom-btn d-inline-block" onclick="return confirm('Êtes-vous sûr de vouloir modifier ce Quiz ?')">{{ button_label|default('Modifier') }}</button>
		<a href="{{ path('app_quiz_index') }}" class="btn btn-success custom-btn d-inline-block ml-2">Retour</a>
	</div>
</div>
{{ form_end(form) }}
