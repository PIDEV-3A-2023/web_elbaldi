
{% extends 'baseBack.html.twig' %}
{% block title %}
Commande 
{% endblock %}
{% block body %}
    <h1>Commande </h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdCmd</th>
                <th>Etat</th>
                <th>DateCmd</th>
                <th>Total</th>
                <th>Adresse</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commande in commandes %}
            <tr>
                <td>{{ commande.idCmd }}</td>
                <td>{{ commande.etat }}</td>
                <td>{{ commande.dateCmd ? commande.dateCmd|date('Y-m-d') : '' }}</td>
                <td>{{ commande.total }}</td>
                <td>{{ commande.adresse }}</td>
                <td>
                    <a href="{{ path('app_commande_show', {'idCmd': commande.idCmd}) }}">Détails</a>
                    <a href="{{ path('app_commande_edit', {'idCmd': commande.idCmd}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
