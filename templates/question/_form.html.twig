{{form_start(form,{'attr':{'novalidate':'novalidate'}})}}
{{ form_widget(form) }}


<div class="form-group row text-center">
	<div class="col-sm-2"></div>
	<div class="col-sm-10 buttons-group">
		<button class="btn btn-success custom-btn d-inline-block ml-2">{{ button_label|default('Ajouter') }}</button>
		{# <a href="{{ path('app_question_index') }}" class="btn btn-success custom-btn d-inline-block ml-2">Retour1</a> #}
		{# <a href="{{ path('app_quiz_index', {'idQuiz': quiz.getIdQuiz()}) }}" class="btn btn-success custom-btn d-inline-block ml-2">Retour</a> #}
		<a href="{{ path('app_question_index', {'idQuiz': quiz.getIdQuiz()}) }}" class="btn btn-success custom-btn d-inline-block ml-2">Retour</a>


	</div>
</div>
{{ form_end(form) }}
