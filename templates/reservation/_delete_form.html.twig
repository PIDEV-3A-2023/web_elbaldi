<form method="post" action="{{ path('app_reservation_delete', {'idReservation': reservation.idReservation}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.idReservation) }}">
    <button class="btn">Supprimer</button>
</form>